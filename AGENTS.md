# AGENTS.md

All project instructions live in **[CLAUDE.md](./CLAUDE.md)** in this same
directory. Read it fully before making any changes — especially the
version-control rules at the top (never commit, push, branch, or open a PR
unless explicitly told to in that same message).

CLAUDE.md is the single source of truth for this repo's architecture,
build/test commands, and non-obvious constraints. This file exists only so
non-Claude agents that look for `AGENTS.md` are pointed there.
