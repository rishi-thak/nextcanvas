# dependencies
node_modules/

# Next.js
.next/
next-env.d.ts
*.tsbuildinfo

# TypeScript build output — regenerated by `npm run build` (prebuilt wasm in
# nextcanvas/swc/ IS committed; the TS dist is not)
nextcanvas/dist/

# Rust build artifacts for the SWC plugin (the prebuilt .wasm in swc/ is committed)
nextcanvas/swc-plugin/target/

# misc
.DS_Store
*.log
